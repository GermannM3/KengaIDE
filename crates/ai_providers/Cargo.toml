[package]
name = "ai_providers"
version = "0.1.0"
edition = "2021"
description = "AI Providers: LocalProvider, ApiProvider"

[dependencies]
async-trait = "0.1"
futures-util = "0.3"
serde = { version = "1", features = ["derive"] }
thiserror = "1"
tracing = "0.1"
