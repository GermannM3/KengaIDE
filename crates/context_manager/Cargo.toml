[package]
name = "context_manager"
version = "0.1.0"
edition = "2021"
description = "Context Manager: сбор контекста, лимиты, токенизация"

[dependencies]
serde = { version = "1", features = ["derive"] }
thiserror = "1"
tracing = "0.1"
